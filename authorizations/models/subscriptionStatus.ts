export enum SubscriptionStatus {
    Active = "active",
    Archived = "archived",
    Deprovisioned = "deprovisioned",
    Disconnected = "disconnected",
    Reserved = "reserved",
    Stale = "stale",
}
